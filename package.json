{
  "name": "vertexgo-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "start": "sh render_inject_env.sh"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
